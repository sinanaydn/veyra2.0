spring:
  application:
    name: veyra-api
  threads:
    virtual:
      enabled: true

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 10000
      idle-timeout: 300000
      max-lifetime: 600000

  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

  lifecycle:
    timeout-per-shutdown-phase: 30s

jwt:
  # JWT_SECRET env var zorunludur — fallback yoktur.
  # HS256 için en az 32 karakter (256 bit) olmalıdır.
  secret: ${JWT_SECRET}
  expiration: 900000              # 15 dakika (ms) — kısa ömürlü access token
  refresh-expiration-days: 7      # 7 gün — refresh token DB'de saklanır, revoke edilebilir

springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: ${SWAGGER_ENABLED:true}
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
    enabled: ${SWAGGER_ENABLED:true}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

server:
  port: 8080
  shutdown: graceful
  compression:
    enabled: true
    min-response-size: 1024
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
  tomcat:
    max-http-form-post-size: 1MB
    max-swallow-size: 1MB

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

admin:
  email: ${ADMIN_EMAIL}
  password: ${ADMIN_PASSWORD}

# --- Object Storage (MinIO dev / Cloudflare R2 prod) ---
# S3 API uyumlu her servis için çalışır. Tek implementation (S3StorageService),
# endpoint + credentials değişince prod'a geçer.
storage:
  s3:
    endpoint: ${STORAGE_S3_ENDPOINT:http://localhost:9000}
    region: ${STORAGE_S3_REGION:auto}
    bucket: ${STORAGE_S3_BUCKET:veyra-cars}
    access-key: ${STORAGE_S3_ACCESS_KEY}
    secret-key: ${STORAGE_S3_SECRET_KEY}
    public-base-url: ${STORAGE_S3_PUBLIC_BASE_URL:http://localhost:9000/veyra-cars}
    path-style-access: ${STORAGE_S3_PATH_STYLE:true}
    auto-create-bucket: ${STORAGE_S3_AUTO_CREATE_BUCKET:true}
